# dependencies
node_modules/

# secrets
.env
.env.*
!.env.example

# deploy state
site/.vercel/
.vercel/

# generated gallery (rebuilt from canon/ on every publish)
site/

# playwright
test-results/
playwright-report/

# os / editor
.DS_Store
*.log

# Runtime data stays local, out of git (owner's decision, 2026-09-24).
# The record is still kept on disk and never deleted (ARCHITECTURE.md, principle 3).
floor/
archive/
canon/
taste.md
